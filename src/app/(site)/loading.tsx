import { LuxuryLoader } from "@/components/shared/LuxuryLoader";

export default function SiteLoading() {
  return (
    <main className="bg-ivory">
      <LuxuryLoader label="Loading page" variant="page" />
    </main>
  );
}
